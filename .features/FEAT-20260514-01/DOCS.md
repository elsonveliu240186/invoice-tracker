# FEAT-20260514-01 — Documentation summary

Generated by the documentation agent on 2026-05-14.

## Files changed

| File | Change | Notes |
|------|--------|-------|
| `docs/CHANGELOG.md` | Prepended entry under `[Unreleased] → Added` | Full narrative covering backend endpoints, zero-fill logic, palette token system, StatusBadge, MarkAsPaidButton, palette switcher, coverage results, security fixes, and review/security iteration counts |
| `docs/FEATURES.md` | Added row for FEAT-20260514-01 in the table; inserted full detail block before FEAT-20260513-03 | Includes overview, backend/frontend change lists, quality gate table, security findings, and open items |
| `docs/ARCHITECTURE.md` | Updated backend C4 component diagram; replaced frontend component diagram; added ADR-019 through ADR-022 | Backend diagram adds `DashboardController`, `DashboardService`, `AppConfig` (Clock bean), `InvoiceStatus` enum, and updated `InvoiceRepositoryAdapter` / Postgres notes; frontend diagram adds `DashboardPage` with stat cards and chart components, `useThemeColor`, `StatusBadge`, `MarkAsPaidButton`, `markInvoicePaid`, `useMarkInvoicePaid`, `paletteStore`, `usePalette`, `PaletteProvider`, `PaletteToggle` |
| `docs/SEQUENCE_DIAGRAMS.md` | Appended `### FEAT-20260514-01` section | Two diagrams: 4.1 happy-path dashboard load; 4.2 edge-case mark-as-paid. Copied from PLAN.md §4 |
| `docs/API.md` | Added Dashboard tag row to endpoint table; added `PATCH /api/v1/invoices/{id}/mark-paid` row; inserted `## Dashboard (FEAT-20260514-01)` section with full response schema; inserted `### PATCH … mark-paid` section with example response and error table; annotated existing `GET /api/v1/invoices` and `GET /api/v1/invoices/{id}` rows to note `status` field is now present | No existing sections removed |
| `postman/collection.json` | Added `Dashboard (FEAT-20260514-01)` folder with `Get Dashboard Stats` request (Basic auth, 3 `pm.test` assertions); added `Mark Invoice as Paid (FEAT-20260514-01)` request inside the `Invoices` folder (after `Get Invoice`, before `Get Invoice PDF`), with 2 `pm.test` assertions checking `200 OK` and `status === 'PAID'` | Existing requests, scripts, and env-var assignments preserved |

## Files not changed

| File | Reason |
|------|--------|
| `postman/local-dev.environment.json` | `invoiceId` variable already present; `baseUrl`, `apiVersion`, `authToken` already wired. No new path variables introduced by FEAT-20260514-01 that require an environment entry. |
| `docs/openapi.json` | Backend is not startable without a running Postgres instance in this environment. Regeneration from live `/v3/api-docs` requires `docker-compose up` followed by `./mvnw spring-boot:run`. The two new endpoints (`GET /api/v1/dashboard/stats`, `PATCH /api/v1/invoices/{id}/mark-paid`) are fully documented in `docs/API.md` as the authoritative human-readable contract. Re-generate `docs/openapi.json` once the stack is running with: `curl -s http://localhost:8080/v3/api-docs | python3 -m json.tool > docs/openapi.json`. |
| `docs/SECURITY.md` | SECURITY.md artefact lives at `.features/FEAT-20260514-01/SECURITY.md` per framework convention. The OWASP findings and resolutions from that report are summarised in `docs/FEATURES.md` under the FEAT-20260514-01 security section. |
| `docs/DESIGN_SYSTEM.md` | Existing token reference document; FEAT-20260514-01 adds tokens to `src/index.css` but the design system document update (adding `--color-sidebar-*`, `--color-chart-*`, `--color-status-*` token descriptions) is deferred to the next DESIGN_SYSTEM.md sweep. The ADR entries in `docs/ARCHITECTURE.md` capture the architectural decisions. |

## Open items carried forward

- `docs/openapi.json` needs regeneration from the live backend (requires Postgres + Spring Boot running).
- `docs/DESIGN_SYSTEM.md` should be updated to document the new `--color-sidebar-*`, `--color-chart-*`, `--color-status-*`, and `--palette-*` tokens, and the `.palette-teal-steel` override class.
- `invoices.fields.status` i18n key missing from `en.json` (non-blocking reviewer finding).
- MSW dashboard mock `revenueByMonth` array has 5 entries; spec requires 6 (non-blocking reviewer finding).
- 15 Playwright E2E specs in `tests/dashboard/` are currently skipped pending a live Vite dev server; remove `test.skip(true, ...)` to activate.
