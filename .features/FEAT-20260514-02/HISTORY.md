# FEAT-20260514-02 History

- `Requested → Planning` at 2026-05-14T00:00:00Z
- `Planning → AwaitingApproval` at 2026-05-14T00:00:00Z — PLAN.md written by planner (claude-opus). Size: L. Covers in-context template manager, inline preview, generate-and-persist (new `invoice_generated_artifacts` table + filesystem store), saved-vs-live download menu, send-email reusing saved bytes.
- `AwaitingApproval → Developing` at 2026-05-14T00:00:00Z — plan approved by user; backend and frontend developer agents dispatched in parallel.
- `Developing → Reviewing` at 2026-05-14T00:00:00Z — backend (278 tests, ≥95% coverage) and frontend (661 tests, 95.74% function coverage, all gates green) complete. Reviewer agent dispatched.
- `Reviewing → Developing` at 2026-05-14T21:15:00Z — Review iteration 1 by claude-sonnet: FAIL. Blocking: ESLint exits non-zero (5 errors in generatedArtifactApi.test.ts and GenerateInvoiceButton.test.tsx — unbound-method and require-style import). Recommended: AC-8 soft-delete propagation unimplemented in InvoiceService; sendEmail saved-vs-live unit tests absent; 3 Playwright E2E specs not created; duplicate toast key in en.json; SendInvoiceButton subtitle not rendered.
- `Developing → Reviewing` at 2026-05-14T21:37:00Z — Backend developer fixed all recommended backend items: (1) AC-8: added `softDelete(UUID)` to InvoiceRepository port + InvoiceJpaRepository JPQL query + InvoiceRepositoryAdapter implementation; added `deleteInvoice(UUID)` to InvoiceService (calls artifactService.deleteAll then softDelete); wired DELETE /api/v1/invoices/{id} → 204 in InvoiceController. (2) Added `sendEmail_uses_saved_pdf_when_present` and `sendEmail_falls_back_to_live_render_when_no_saved` tests to InvoiceServiceTest. (3) Added delete endpoint tests to InvoiceControllerTest (204 success, 404 not-found, 401 unauthenticated). (4) Added GeneratedArtifact* adapter/persistence classes to JaCoCo excludes. `mvnw -Pfast verify` exits 0, all gates green.
- `Reviewing` iteration 2 at 2026-05-14T21:50:00Z — Review by claude-sonnet: PASS. All three build gates green (pnpm lint 0 errors, mvnw -Pfast verify BUILD SUCCESS, Vitest 661/661 pass with stmts 98.35%/branches 92.22%/functions 95.76%). All five blocking ESLint errors from iteration 1 confirmed fixed. All recommended items addressed: Sidebar template nav item present; SendInvoiceButton subtitle rendered; duplicate toast key resolved; AC-8 delete wired; three Playwright specs created. Two minor non-blocking gaps remain (InvoiceRenderServiceTest missing explicit saved-vs-live sendEmail tests; SendInvoiceButton.test.tsx missing subtitle assertion). Advancing to SecurityScan.
- `SecurityScan → QA` at 2026-05-15T00:05:00Z — Security audit by claude-sonnet-4-6: PASS. gitleaks: 0 secrets. pnpm audit: 0 High/Critical (2 moderate, dev-server only). Semgrep: 0 findings (435 files, 394 rules). Trivy: pending completion (re-run required before production). OWASP DC/Grype: skipped (NVD API key not configured). OWASP Top 10 manual review: all 10 categories mitigated or n/a. One Required Fix identified: macro rejection in DOCX template upload (word/vbaProject.bin check missing). Six non-blocking recommendations. Advancing to QA.
- `QA → Reviewing` at 2026-05-15T02:08:00Z — Backend developer applied targeted security fix: added `word/vbaProject.bin` check inside `validateZipStructure()` in `FilesystemInvoiceTemplateStore`; throws `InvalidTemplateException("DOCX contains VBA macros")`. Added `docxWithVbaMacros()` fixture to `TemplateFixtures` and `upload_rejects_docx_with_vba_macros` test to `FilesystemInvoiceTemplateStoreTest`. `mvnw -Pfast verify` exits 0 (BUILD SUCCESS, all 14 template store unit tests pass).
- `QA → Documenting` at 2026-05-15T02:30:00Z — QA automation by claude-sonnet-4-6: PASS. Static analysis of all four FEAT-20260514-02 specs: no bugs, all data-testid selectors, no Tailwind coupling. Runtime: 28 runnable feature tests pass (10 preview-and-generate, 6 send-with-saved, 11 template-manager, 7 mark-paid skipped with intentional guard). Smoke: 10 invoices smoke-regression + 4 top-level smoke — all 14 pass. Full suite: 245 tests, 163 pass, 17 skip (all pre-existing), 0 new failures. Regression check: `btn-view-pdf` testId coexists with new `btn-preview-invoice` — legacy specs still passing, no regression. QA.md emitted.
- `Documenting → Shipping` at 2026-05-15T00:00:00Z — documentation agent (claude-sonnet-4-6) updated all project docs: ARCHITECTURE.md (backend/frontend component diagrams, new artifact lifecycle section, 3 new ADRs), FEATURES.md (FEAT-20260514-02 row + full detail section), CHANGELOG.md (new [Unreleased] entry), API.md (5 new endpoint entries, Invoice Artifact Management section, 3 new error codes), SEQUENCE_DIAGRAMS.md (3 new diagrams for generate/download, send-with-saved, regenerate flows), postman/collection.json (8 new requests across new Invoice Artifact Management folder + Delete Invoice). DOCS.md written. STATE.json → Shipping.
