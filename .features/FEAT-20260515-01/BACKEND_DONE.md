Implemented company profile singleton (Flyway V9, domain record, JPA entity, service with config fallback, GET/PUT controller at /api/v1/settings/company), invoice update endpoint (PUT /api/v1/invoices/{id} with DRAFT-only guard and InvoiceNotEditableException → 409), auto-generated invoice number sequencing (INV-YYYY-NNNN with pg_advisory_xact_lock), InvoiceArtifactService wired to CompanyProfileService for render properties, and all required tests; `mvnw -Pfast verify` exits 0 with JaCoCo ≥ 90% line + branch on the merged exec.
