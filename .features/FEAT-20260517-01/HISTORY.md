# FEAT-20260517-01 History

- `Requested → Planning` at 2026-05-17T00:00:00Z
- `Planning → AwaitingApproval` at 2026-05-17T00:00:00Z — PLAN.md written by claude-opus
- `AwaitingApproval → Developing` at 2026-05-17T08:15:00Z — plan approved (amended to include dashboard date filter + filter icon); backend and frontend developer agents dispatched in parallel
- `Developing → Reviewing` at 2026-05-17T09:43:26Z — backend implementation complete; `mvnw verify` exits 0, all 68 tests pass (4 skipped: LibreOffice + 2 render skips), JaCoCo merged branch coverage passes 90% gate. Delivered: MonthlyExpense domain record, ExpenseRepository port methods (expenseByMonth, expenseByCategoryInRange), native JPA queries in ExpenseJpaRepository, ExpenseRepositoryAdapter delegation, CategoryExpense + ExpenseStatsResponse DTOs, DashboardService.getExpenseStats (6-month default, 24-month max, zero-fill), DashboardController GET /api/v1/dashboard/expense-stats + optional from/to on existing stats endpoint, V13 Flyway migration (functional index on expenses.expense_date), AuthRateLimitFilter capacity made configurable via @Value.
- Frontend implementation complete at 2026-05-17T10:15:00Z — `pnpm vitest run --coverage` exits 0 (global thresholds pass), `pnpm build` exits 0, `pnpm lint` 0 errors. Delivered: MonthlyExpense/CategoryExpense/DashboardExpenseStats types; getDashboardExpenseStats API function with optional from/to params; useDashboardExpenseStats hook; ExpenseByMonthChart (BarChart with formatExpenseDollar/makeExpenseTooltipFormatter exports); ExpenseByCategoryChart (PieChart donut with makeExpenseLabelRenderer/makeCategoryTooltipFormatter/FALLBACK_COLORS exports); DashboardDateFilter popover (Apply/Clear only — outside-click blocked via onInteractOutside); useDashboardStats updated to accept from/to; DashboardPage updated with filter state + expense section; MSW handler for /api/v1/dashboard/expense-stats; en.json i18n keys; ResizeObserver + PointerEvent polyfills in test-setup. 113 test files pass, 982 tests pass (5 pre-existing failures in ViewPdfButton + InvoiceDetailPage are pre-existing).

- `Reviewing → SecurityScan` at 2026-05-17T12:00:00Z — iteration 2 review passed (status: pass). All 3 blocking issues from iteration 1 resolved: DashboardService.getStats now applies date filtering via countByStatusInRange/revenueByStatusInRange/revenueByMonthInRange; Postman collection updated with Get Expense Stats request; Playwright E2E spec tests/dashboard/expense-charts.spec.ts created. 4 non-blocking recommendations filed.
- `SecurityScan -> QA` at 2026-05-17T16:00:00Z -- security audit passed (status: pass). gitleaks: 0 secrets. pnpm audit: 0 High/Critical (2 moderate, dev-toolchain only). Semgrep: 0 findings. OWASP Top 10: all mitigated or n/a. 4 non-blocking recommendations filed.
- `2026-05-17T18:00:00Z` product-owner: rejected — changelog entry missing from docs/CHANGELOG.md and no DOCS.md draft exists; all three quality gates pass, scope complete, zero open required items; re-run after documentation agent writes DOCS.md with changelog entry.
- `Documenting → Shipping` at 2026-05-17T19:00:00Z — documentation agent complete. Written: `.features/FEAT-20260517-01/DOCS.md` (feature summary, API contract, component table, test coverage, security notes, changed-files list). Updated: `docs/CHANGELOG.md` (FEAT-20260517-01 entry prepended under [Unreleased]); `docs/FEATURES.md` (new row in table + detail section); `docs/API.md` (new endpoint row in summary table, updated stats endpoint note, full `GET /api/v1/dashboard/expense-stats` detail section). STATE set to Shipping.
- `2026-05-17T20:00:00Z` product-owner: approved (iteration 2) — all gates pass (Review iter 2, Security iter 1, QA iter 1); zero open required items; all 9 ACs implemented; no undeclared user-facing scope creep; CHANGELOG entry present and accurate; no PR yet (devops pending).
- `Shipping → Done` at 2026-05-17T20:30:00Z — devops agent committed gate artefacts + docs (aeacb48), pushed branch `feat/expense-dashboard-charts` to origin, opened PR https://github.com/elsonveliu240186/invoice-tracker/pull/11. STATE set to Done.
